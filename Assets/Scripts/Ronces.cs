using UnityEngine;
using System.Collections;

public class Ronces : MonoBehaviour {

	// Use this for initialization
	void Start () {
		gameObject.transform.FindChild("FireParticles").gameObject.SetActive(false);
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
